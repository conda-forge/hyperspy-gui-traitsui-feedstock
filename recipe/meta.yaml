{% set name = "hyperspy-gui-traitsui" %}
{% set filename = "hyperspy_gui_traitsui" %}
{% set version = "1.2" %}
{% set sha256 = "0569d1d6c8b7b145dfa8536a59265a5a7895493f2287f20b90f397ddcdc89e47" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ filename }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ filename }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}


build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv "

requirements:
  build:
    - python >=3.5
    - pip

  run:
    - python >=3.5
    - pyqt
    - traitsui >=6.0
    - hyperspy-base >=1.5

test:
  commands:
    - export MPLBACKEND="agg"
    - pytest --pyargs hyperspy_gui_traitsui

  requires:
    - pytest

about:
  home: https://github.com/hyperspy/hyperspy_gui_traitsui
  license: GPL v3
  summary: "traitsui GUI elements for HyperSpy."
  license_file: LICENSE 

extra:
  recipe-maintainers:
    - to266
    - francisco-dlp
